<div class="frame">
    <div class="center">
          <figure class="shoe left"></figure>
          <figure class="shoe right"></figure>
    </div>
</div>
<style>
    .frame {
  position: relative;
  width: 400px;
  height: 400px;
  border-radius: 8px;
    box-shadow: 4px 8px 16px 0 rgba(0,0,0,0.1);
    overflow: hidden;
  background: linear-gradient(to bottom, blanchedalmond 60%, black 40%);
}

.center {
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%,-50%);
	display: flex;
	justify-content: center;
	align-items: center;
	animation: appear 3s ease-out;
}

.shoe {
	margin: 0;
	position: absolute;
	width: 150px;
	height: 150px;
	background: url('https://100dayscss.com/codepen/doc-martens.svg') center center no-repeat;
	transition: all;
	animation: walk 2s infinite ease-in-out;
}

.left {
	animation-delay: 1s; 
}

@keyframes appear {
	from {
		opacity: 0;
	}
	55% {
		opacity: 0;
	}
	to {
		opacity: 1;
	}
}

@keyframes walk {
    0%, 100% {
        transform: translate(90px, -30px) rotateZ(-30deg);
    }
    18% {
        transform: translate(60px, 0) rotateZ(-5deg);
    }
    50% {
        transform: translate(-70px, -30px) rotateZ(40deg);
    }
}
</style>